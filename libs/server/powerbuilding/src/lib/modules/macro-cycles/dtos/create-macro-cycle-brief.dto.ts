import { ApiProperty } from '@nestjs/swagger';
import { IsString } from 'class-validator';
import { Expose } from 'class-transformer';
import {
  IMacroCycleModel,
  TBaseModelAutogeneratedProps,
  TMacroCycleModelArrayModelProps,
} from '@powerbuilding-trainer/shared/core';

type TCreateMacroCycleBriefDTO = Omit<
  IMacroCycleModel,
  TBaseModelAutogeneratedProps | TMacroCycleModelArrayModelProps
>;

export class CreateMacroCycleBriefDTO implements TCreateMacroCycleBriefDTO {
  @ApiProperty()
  @Expose()
  @IsString()
  public name: string;

  constructor(dto: CreateMacroCycleBriefDTO) {
    Object.assign(this, dto);
  }
}
